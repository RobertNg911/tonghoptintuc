---
phase: 06-content-prompt
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified:
  - generate.js
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-03
must_haves:
  truths:
    - "AI generates viral-style content matching Gen Z Vietnamese social media tone"
    - "Content follows the hybrid writing style (satire + Gen Z humor)"
    - "Output is 300-500 words with proper structure (hook, body, ending)"
artifacts:
  - path: generate.js
    provides: AI content generation with improved prompt
    exports: generateWithRetry()
    min_lines: 80
key_links:
  - from: generate.js
    to: Groq API
    via: axios.post with system + user messages
    pattern: groq.*chat/completions
---

<objective>
Cải thiện prompt cho generate.js để tạo content viral, hấp dẫn cho Facebook posts. Sử dụng system prompt thay vì inline prompt, điều chỉnh parameters cho phù hợp với hybrid Gen Z Vietnamese + Heavy satire style.
</objective>

<context>
@.planning/phases/06-content-prompt/06-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Key decisions from 06-CONTEXT.md:
# - System Prompt: Define AI role là "Content Creator bẩn"
# - User Prompt: Chỉ cung cấp tin cụ thể (title, source)
# - Tone: Hybrid - Gen Z + Deep satire
# - Humor: Heavy (drama queen, sarkasmus max)
# - Format: Flexible - tùy loại tin
# - Hook: Bold claim
# - Body: Narrative
# - Ending: Share-bait
# - Length: 300-500 words
# - Temperature: 0.8-0.9
# - Max tokens: ~800
</context>

<tasks>

<task type="auto">
  <name>task 1: Refactor generate.js to use System Prompt</name>
  <files>generate.js</files>
  <action>
    Update generate.js để sử dụng system prompt và user prompt tách biệt:

    1. **Tách prompt thành system và user:**
    ```javascript
    const SYSTEM_PROMPT = `Bạn là một Content Creator chuyên nghiệp, viết content "bẩn" (hài hước) cho mạng xã hội Việt Nam.

VAI TRÒ & PHONG CÁCH:
- Bạn viết theo phong cách Hybrid: Gen Z Vietnam + Châm biếm sâu (satire)
- Tone: Heavy drama - sarkasmus max, câu view cực đoan
- Đối tượng: Mainstream - ai đọc cũng hiểu
- Giá trị: Insightful jokes - vừa cười vừa suy nghĩ

CẤU TRÚC BÀI VIẾT:
1. HOOK (dòng đầu tiên):
   - Bold claim bất ngờ, ngược đời
   - Logic ngược, gây shock nhẹ
   - Đánh vào tò mò người đọc

2. BODY (thân bài):
   - Narrative: kể story từ đầu, dramatic
   - Bóc trần sự thật, expose
   - So sánh ngược đời (Đem so sánh A với B - người ta nói X nhưng thực tế Y)
   - Có insight, có giá trị

3. KẾT BÀI:
   - Share-bait: câu chốt để người đọc tag bạn bè
   - Câu hỏi tương tác
   - Hoặc drama câu chốt

FORMAT:
- Emoji tự nhiên (dùng nếu phù hợp, không spam)
- Dòng ngắn, có spacing (mobile-friendly)
- Paragraph long-form như viết báo
- 3-5 hashtags tiếng Việt
- 100% tiếng Việt có dấu

ĐỘ DÀI: 300-500 từ`;

    const USER_PROMPT = (item) => `Viết một bài đăng Facebook về tin này:
"${item.title}"
Nguồn: ${item.source}`;

    async function generateContent(item) {
      const response = await axios.post(
        'https://api.groq.com/openai/v1/chat/completions',
        {
          model: 'llama-3.3-70b-versatile',
          messages: [
            { role: 'system', content: SYSTEM_PROMPT },
            { role: 'user', content: USER_PROMPT(item) }
          ],
          temperature: 0.85,
          max_tokens: 800
        },
        { headers: { 'Authorization': `Bearer ${GROQ_API_KEY}` } }
      );
      
      if (!response.data.choices?.[0]?.message?.content) {
        throw new Error('AI response empty');
      }
      
      return response.data.choices[0].message.content;
    }
    ```
  </action>
  <verify>
    <automated>grep -q "role.*system" generate.js && grep -q "SYSTEM_PROMPT" generate.js && grep -q "temperature.*0.85" generate.js && echo "PROMPT_REFACTORED"</automated>
  </verify>
  <done>generate.js refactored with system prompt structure</done>
</task>

<task type="auto">
  <name>task 2: Update AI parameters</name>
  <files>generate.js</files>
  <action>
    Update các tham số AI trong generate.js:

    1. **Temperature:** 0.85 (creative, viral-friendly)
    2. **Max tokens:** 800 (đủ cho 300-500 words output)
    3. **Có thể thêm top_p:** 0.9

    ```javascript
    const response = await axios.post(
      'https://api.groq.com/openai/v1/chat/completions',
      {
        model: 'llama-3.3-70b-versatile',
        messages: [
          { role: 'system', content: SYSTEM_PROMPT },
          { role: 'user', content: USER_PROMPT(item) }
        ],
        temperature: 0.85,
        top_p: 0.9,
        max_tokens: 800
      },
      { headers: { 'Authorization': `Bearer ${GROQ_API_KEY}` } }
    );
    ```
  </action>
  <verify>
    <automated>grep -q "temperature.*0.85" generate.js && grep -q "top_p.*0.9" generate.js && grep -q "max_tokens.*800" generate.js && echo "PARAMS_UPDATED"</automated>
  </verify>
  <done>AI parameters updated: temperature 0.85, top_p 0.9, max_tokens 800</done>
</task>

<task type="auto">
  <name>task 3: Verify output quality</name>
  <files>generate.js</files>
  <action>
    Sau khi update, kiểm tra output bằng cách:
    
    1. Chạy thử với một tin mẫu:
    ```bash
    # Tạo news.json test
    echo '[{"title":"OpenAI announces new AI model with unprecedented capabilities","source":"TechCrunch","link":"https://test.com","pubDate":"2026-04-24"}]' > news.json
    
    # Chạy generate.js
    node generate.js
    
    # Kiểm tra output
    cat content-1.txt
    ```

    2. Checklist kiểm tra:
    - [ ] Hook có bold claim không?
    - [ ] Tone có drama/satire không?
    - [ ] Độ dài 300-500 words?
    - [ ] Format mobile-friendly?
    - [ ] Có hashtags?

    3. Nếu quality chưa tốt → điều chỉnh prompt
  </action>
  <verify>
    <automated>test -f content-1.txt && wc -w content-1.txt | awk '{if ($1 >= 300 && $1 <= 500) exit 0; else exit 1}' && echo "QUALITY_VERIFIED"</automated>
  </verify>
  <done>Output quality verified: 300-500 words, viral style</done>
</task>

</tasks>

<verification>
- [ ] generate.js has system prompt defined
- [ ] generate.js uses system + user messages structure
- [ ] Temperature set to 0.85
- [ ] Max tokens set to 800
- [ ] Output is 300-500 words
- [ ] Content has viral style (hook, body, ending)
</verification>

<success_criteria>
1. System prompt defines AI role as "Content Creator bẩn"
2. Content follows hybrid Gen Z + Heavy satire style
3. Output is 300-500 words with bold hook, narrative body, share-bait ending
4. Temperature 0.85 for creative/viral output
5. Max tokens 800 for adequate output length
</success_criteria>

<output>
After completion, update `.planning/phases/06-content-prompt/06-SUMMARY.md`
</output>

---

*Plan: 01*
*Context: 06-CONTEXT.md*
*Created: 2026-04-24*